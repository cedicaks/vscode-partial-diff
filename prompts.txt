PROMPT1:

1. Add a feature to save a comparison result tab to file: .html and .diff

PROMPT2:

1. In the html file only show 1 vertical scroll bar. The left part doesn't need scroll bar, because one scroll bar on the right is enough.
2. At the bottom of the html page show a information section that (which look nice):
    a. Show resume of the differences:
        - How many lines are added
        - How many lines are removed
        - How many lines are modified
3. Make the scroll bar on the right just like what the comparison tabs like it seen in the vscode, which contain a green / red / .. mark to see where is the differences (replace the standard scrollbar). So viewer can quickly tell where are the differences

PROMPT3:

1. Make the bottom (horizontal) scrollbars always shown (not only when the page is scrolled to the bottom)
2. Make the bottom scrollbar has color theme like the vertical scrollbar. (dark)
3. Make the vertical scrollbar wider (3x current width)


PROMPT4:

1. Make the html output left & right file name display easy to be renamed in the html file. The renaming will be done using a text editor.

PROMPT5:

1. Add checkbox at the bottom section "Show only differences" to show only differences with some lines before and after. Show the differences separated by thin line in this mode.
2. Add a button "Swap" to swap left section and the right section. This also modify the diff "Add" and "Remove" result (also switched).