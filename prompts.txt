PROMPT1:

1. Add a feature to save a comparison result tab to file: .html and .diff

PROMPT2:

1. In the html file only show 1 vertical scroll bar. The left part doesn't need scroll bar, because one scroll bar on the right is enough.
2. At the bottom of the html page show a information section that (which look nice):
    a. Show resume of the differences:
        - How many lines are added
        - How many lines are removed
        - How many lines are modified
3. Make the scroll bar on the right just like what the comparison tabs like it seen in the vscode, which contain a green / red / .. mark to see where is the differences (replace the standard scrollbar). So viewer can quickly tell where are the differences

PROMPT3:

1. Make the bottom (horizontal) scrollbars always shown (not only when the page is scrolled to the bottom)
2. Make the bottom scrollbar has color theme like the vertical scrollbar. (dark)
3. Make the vertical scrollbar wider (3x current width)


PROMPT4:

1. Make the html output left & right file name display easy to be renamed in the html file. The renaming will be done using a text editor.

PROMPT5:

1. Add checkbox at the bottom section "Show only differences" to show only differences with some lines before and after. Show the differences separated by thin line in this mode.
2. Add a button "Swap" to swap left section and the right section. This also modify the diff "Add" and "Remove" result (also switched).

PROMPT6:

1. Make the separation of differences (in show only differences mode) clearer, increase the thickness of the line and add a bit space above and below the separator line 

PROMPT7:

1. Why after clicking the Swap button the page is not responding for several seconds?
2. The same thing also happen if the browser tab containing the page (brave) to become a new window

PROMPT8:

11. Just like the PROMPT4. Add an 'initial-position' setting (initially swapped or not) as a boolean setting. if this value is true then the diff will initially show swapped state.

PROMPT9:

1. In the "safe to file" feature. It looks like the comparison think the line ended with \r and \r\n is different although the line content is the same. Because when I use the PartialDiff: Compare Text With Clipboard the comparison is good (only different line is detected), but when I use the PartialDiff: Save Comparison Result to File, it says all the lines are different. Check it and fix it.

PROMPT10:

1. I think the logic of comparing slightly difference between the "PartialDiff: Compare Text with Clipboard" and the "PartialDiff: Save Comparison result to a file". For example comparing: (---- is the delimiter)
----
  btn2: 
    07 F0 01 20 09
    07 B8 02 20 09
    07 80 03 20 09
    07 48 04 20 09
    07 10 05 20 09
    07 D8 05 20 09
    07 A0 06 20 09
    07 68 07 20 09
    07 30 08 20 09
    00 30 08 20 09
---- 

with 

----
  btn2: 
    07 F0 01 20 09
    07 B8 02 20 09
    07 80 03 20 09
    07 48 04 20 09
    07 10 05 20 09
    tambah
    tambah+
    07 D8 05 20 10
    07 A0 06 20 09
    07 68 07 20 09
    07 30 08 20 09
    00 30 08 20 09
----

the "Save Comparison result to a file" first mark remove the "07 D8 05 20 09" line first followed by add the "tambah"* lines. but the "Compare Text with Clipboard" add the "tambah"* lines first followed by mark "07 D8 05 20 09" as removed. I want to the comparison algorithm of "Save comparison result to a file" is duplicate of the "Compare Text With clipboard" algorithm.

2. Just like in the "Compare text with clipboard', I want the "Save comparison result to a file" results also show / highlight the exact position of characters of a line that is changed (not just remove entire line and add whole new line, but the change is merely only a few characters)
